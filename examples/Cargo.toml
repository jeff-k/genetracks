[package]
name = "genome_demo"
version = "0.1.0"
edition = "2024"

[dependencies]
leptos = { version = "0.8", features = ["csr", "nightly"] }
console_error_panic_hook = "0.1"

genetracks = { path = "../../genetracks" }

[profile.release]
lto = true
codegen-units = 1
